FLASK_ENV=development
SECRET_KEY=cambia-esto-en-produccion
# PostgreSQL (Docker Compose). En local sin Docker puedes omitirlo y se usará SQLite.
DATABASE_URL=postgresql://lealink:lealink@postgres:5432/lealink
