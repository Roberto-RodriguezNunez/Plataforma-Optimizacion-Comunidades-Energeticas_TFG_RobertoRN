FLASK_ENV=development
SECRET_KEY=cambia-esto-en-produccion
# La base de datos no se pone aquí: con Docker la fija docker-compose.yml
# (PostgreSQL); en local sin Docker se usa SQLite automáticamente.
